# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{source/.+\.(haml)$})
  watch(%r{source/stylesheets/.+\.(css|scss)$})
  watch(%r{source/javascripts/.+\.(js|coffee)$})
end
